import { TransactionByCategoryPipe } from './transaction-by-category.pipe';

describe('TransactionByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
